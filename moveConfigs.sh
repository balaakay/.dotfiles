#!/bin/bash

# This script needs to take the files/directories in the .dotfiles path and move
# them to the correct place for .config paths according to each tool/application.
#
# This should also be based on OS. I would like to keep this same script for MacOS
# and for Linux.
